# Tasks to Do

## Back-End
1. Create Direct Access to Database [ADMIN]
    - Be able to add + delete entries (rows) for all tables
    - UI (front-end) where admin is able to see and select all tables (find a way to delete & edit)
    - ONE PAGE (select table, drop down of all data entries)

2. Create Direct Access to User Entries [ADMIN]
    - Admin should see database of all user entries


## Queries
At least 20 SQL queries [Some Ideas]

    1. Create joins of all tables (filter all resources by days)
        - Q1: Monday
        - Q2: Tuesday
        - Q3: Wednesday
        - Q4: Thursday
        - Q5: Friday
    2. Create joins of all tables (filter all resources by location)
    3. INSERT/DELETE/UPDATE